@using Microsoft.AspNetCore.Identity
@model List<IdentityUser> 

<h3>Users</h3>

<ul>
@foreach (var item in Model)
{
    <li>
        @item.UserName | <button type="button" class="btn btn-secondary"><a asp-action="Edit" asp-route-id="@item.Id">Asignar Role</a></button>   
    </li>
    <br>
}
</ul>